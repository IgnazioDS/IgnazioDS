# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Environment / secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key

# Python (the conformance validator is stdlib-only, but keep caches out)
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/

# Node (none committed here, but guard against accidental scaffolding)
node_modules/
dist/
*.log
